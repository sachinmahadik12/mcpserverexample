add the following installation steps

#installation steps
'''json

    {
  "mcpServers": {
    "add": {
      "command": "uvx",
		"args": [
		  "--from",
		  "git+https://github.com/sachinmahadik12/mcpserverexample.git",
		  "mcp-server"
		  ]
    }
  }
}
